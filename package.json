{
  "name": "my-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check:content": "node --no-warnings scripts/check-content.mjs",
    "check:assets": "node --no-warnings scripts/check-assets.mjs",
    "check:structure": "node --no-warnings scripts/check-structure.mjs",
    "verify": "npm run check:content && npm run check:assets && npm run check:structure && npm run build"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "next": "16.2.2",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4"
  }
}
